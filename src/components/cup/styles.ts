import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    zIndex: 1,
  },
  smoke: {
    zIndex: 2,
    bottom: 192,
    position: 'absolute',
    alignItems: 'center',
  },
  image: {
    zIndex: 1,
    marginBottom: -64,
  },
})
