import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    zIndex: 2,
    bottom: 82,
    position: 'absolute',
    alignItems: 'center',
  },
  canvas: {
    width: 116,
    height: 280,
  },
})
